import styles from "./index.module.css";

const HomePage = () => {
  return (
    <section className={styles.homeSection}>
      <h1 className={styles.title}>Hello Change</h1>
    </section>
  );
};

export default HomePage;
